import { Bullet } from '../PhaserGame/gameObjects/Projectiles';
import Phaser from 'phaser';
jest.mock('phaser');




it('Testing Bullet Constructor', () => {
    const bullet = new Bullet();
    
   // expect(PlayScene).toHaveBeenCalledTimes(1);
  });